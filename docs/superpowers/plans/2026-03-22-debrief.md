# Debrief Skill Implementation Plan

**Status:** Completed (2026-03-22) — archived execution plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the `/debrief` session-end skill — a 6-phase sequential pipeline that cleans up loose ends, captures knowledge through 5 engineering disciplines, routes findings to the right persistence targets, and commits everything cleanly.

**Architecture:** Single SKILL.md file containing the full 6-phase workflow (~1200 words target, under 1500 word budget). Enhancement lenses and routing details are inlined rather than split to supporting files, since they're core to the audit logic. Eval-set.json provides trigger test coverage.

**Tech Stack:** Markdown (SKILL.md), JSON (eval-set.json), npm scripts (validate, stats, link, eval)

**Reference:** Design spec at `docs/superpowers/specs/2026-03-22-debrief-design.md`

---

### Task 1: Scaffold the Skill Directory

**Files:**
- Create: `skills/debrief/SKILL.md` (scaffold only)

- [ ] **Step 1: Run the create script**

```bash
npm run create -- debrief "Use when ending a work session to clean up loose ends, capture knowledge through the 5 engineering disciplines, update documentation, and commit changes. Pairs with /recon as session bookends."
```

- [ ] **Step 2: Verify the scaffold was created**

```bash
ls skills/debrief/
```

Expected: `SKILL.md` exists with valid frontmatter.

- [ ] **Step 3: Run validate to confirm scaffold passes**

```bash
npm run validate
```

Expected: 0 errors. May show word count warning (scaffold is minimal).

- [ ] **Step 4: Commit scaffold**

```bash
git add skills/debrief/SKILL.md
git commit -m "scaffold: create debrief skill directory"
```

---

### Task 2: Write the SKILL.md Content

**Files:**
- Modify: `skills/debrief/SKILL.md`

This is the core task. Compress the design spec into an instruction-dense SKILL.md under 1500 words. The recon skill is 1140 words for reference — target ~1200 words.

**Key structural decisions:**
- Frontmatter: `name: debrief`, description starts with "Use when..."
- Overview: 2-3 sentences establishing the skill's purpose and the recon pairing
- Phase architecture: 6 phases listed as a compact reference block
- Each phase: concise instructions with the key rules inline
- Enhancement lenses: integrated into Phase 4 section, not a separate document
- Routing table: inline in Phase 5
- Common mistakes: table format (matches recon's pattern)
- No flowchart needed — the phase list is linear and self-explanatory

- [ ] **Step 1: Write the frontmatter and overview**

Replace the scaffold content with:

```markdown
---
name: debrief
description: "Use when ending a work session to clean up loose ends, capture knowledge through the 5 engineering disciplines, update documentation, and commit changes. Pairs with /recon as session bookends."
---

# Debrief

Session-end skill. Cleans up loose ends, captures knowledge through the 5 engineering disciplines, routes findings to persistence targets, and commits everything cleanly. Pairs with `/recon` — recon opens sessions, debrief closes them.

**Always runs all 6 phases in order.** Parallel tool calls within a phase are encouraged for speed.
```

- [ ] **Step 2: Write the phase architecture block**

Add immediately after the overview:

```markdown
## Phase Architecture

Phase gates are strict — complete one before starting the next.

| Phase | Name | Action |
|-------|------|--------|
| 1 | ASSESS | Scan git status, git diff, TODOs/debug output in uncommitted changes. Categorize as: **Trivial** (formatting, debug cruft, staging) · **Substantive** (failing tests, lint errors) · **Deferred** (too big for session-end). |
| 2 | TRIAGE | Trivial → auto-fix silently. Substantive → prompt user `(y/N)`, default defer. Deferred → log for next session. **Never modify logic, delete files, or alter public APIs without approval.** Present cleanup summary when done. |
| 3 | COMMIT WORK | Auto-commit user's work + triage fixes. Match repo's commit style. If pre-commit hook fails → present error, ask user. If user declines → proceed with uncommitted changes, note in report. **Guard:** if last commit starts with `debrief:`, skip Phases 1-3. |
| 4 | DISCIPLINE AUDIT | Single pass across 5 disciplines + enhancement lenses. See below. |
| 5 | ROUTE KNOWLEDGE | Write findings to correct targets. See routing table below. |
| 6 | COMMIT & REPORT | Auto-commit debrief's doc/memory changes. Output terse action summary. |
```

- [ ] **Step 3: Write Phase 1 ASSESS details**

Add the fast-check rules:

```markdown
---

## Phase 1-2: Assess & Triage

**Checks (parallel where possible):**
- `git status` + `git diff` — uncommitted changes, untracked files
- Lint/test — only if project has a fast check (<10s). Skip slow suites.
- `TODO`/`FIXME` in `git diff` output (uncommitted only, not pre-existing)
- Debug output (`console.log`, `print()`) in `git diff` — only flag if not in logging/CLI code

**Triage rules:**

| Category | Action |
|----------|--------|
| **Trivial** | Auto-fix: format, stage tracked files, remove debug cruft |
| **Substantive** | Prompt: "Fix now? (y/N)" — default defer, execute only on explicit yes |
| **Deferred** | Log as next-session item, no action |

After triage, present cleanup summary: what was resolved, what was deferred.
```

- [ ] **Step 4: Write Phase 4 Discipline Audit**

This is the core differentiator. Include the base audit table and enhancement lenses:

```markdown
---

## Phase 4: Discipline Audit

Single pass asking: "Did anything this session change how agents should operate here?"

| Discipline | Signals | Example |
|------------|---------|---------|
| **Prompt Craft** | New guardrails, output format preferences, agent instruction patterns | "Test names must use `should_X_when_Y` format" |
| **Context Engineering** | Critical files identified, missing context that caused problems, noisy context | "src/config/flags.ts is essential context for feature work" |
| **Intent Engineering** | Trade-offs decided, priorities clarified, agent boundaries set | "Optimize for read performance over write" |
| **Specification Engineering** | Acceptance criteria refined, edge cases discovered | "Empty input returns [], not null" |
| **Task Engineering** | Decomposition patterns, dependency ordering, scope lessons | "Auth and billing parallel, both block dashboard" |

### Enhancement Lenses (apply during the same pass)

1. **Cross-Category** — Chain findings across disciplines. When a finding touches one discipline, grep CLAUDE.md and memory for related entries in others.
2. **Judgment Line** — Check for session moments that clarify human/agent boundaries. Look for corrections like "don't do X automatically" or "always ask before Y."
3. **Time-Bridging** — Flag information with future relevance (3+ months). Scan for dates, deadlines, version numbers, "until," "expires." Route to memory with time context.
4. **Compounding Test** — "Will this make the next session better?" If yes, capture. If it's just a record of what happened, skip. This prevents memory bloat.
5. **Capture Consistency** — Before writing, read MEMORY.md index to check for existing entries. Tag each finding: discipline, routing target, new vs update.

**Rules:**
- Zero findings is valid. Don't force output on quiet sessions.
- Each finding must be **actionable** — not "we discussed auth" but "auth tokens rotate every 24h, not 72h."
- For large repos: parallelize file reads within this phase.
```

- [ ] **Step 5: Write Phase 5 Routing and Phase 6 Commit & Report**

```markdown
---

## Phase 5: Route Knowledge

| Target | What Goes Here | How |
|--------|---------------|-----|
| **CLAUDE.md** | Conventions, guardrails, coding patterns | Append to relevant section |
| **Memory files** | Decisions, project context, preferences, time-bridging signals | Write/update in `~/.claude/projects/.../memory/`, update MEMORY.md. Use frontmatter: `name`, `description`, `type` (user/feedback/project/reference) |
| **Todo** | Deferred loose ends, large work items | Append to project's existing todo file. Don't create orphan files. |
| **Existing docs** | Corrections filling gaps or fixing contradictions | Edit in place |

**Rules:** Update over create. Minimal edits — append, don't restructure. No duplicates — read before writing. **Priority:** memory files over CLAUDE.md when either could work.

---

## Phase 6: Commit & Report

**Commit** debrief's doc/memory changes (triage fixes already committed in Phase 3):

```
debrief: session wrap-up

Knowledge captured:
- [updates with discipline tags]

Deferred:
- [next-session items]
```

**Report** — terse, adaptive. Omit empty sections:

```
SESSION CONTEXT
- [2-3 bullets of what the session was about]

DEBRIEF ACTIONS
Loose ends resolved:
- [triage actions]

Knowledge routed:
- [file: description [Discipline]]

Deferred to next session:
- [items]

Commits:
- [hash] "message" (user work)
- [hash] "message" (debrief updates)
```

Light sessions shrink to 3-4 lines.
```

- [ ] **Step 6: Write Common Mistakes section**

```markdown
---

## Common Mistakes

| Mistake | Correction |
|---------|-----------|
| Rehashing the session | Lead with debrief's actions. Session context is 2-3 bullets max. |
| Capturing obvious things | Compounding test: "Will this make the next session better?" |
| Bloating CLAUDE.md | Append lines, don't restructure. Prefer memory files if CLAUDE.md is long. |
| Duplicating knowledge | Read before writing. Already documented → skip. |
| Committing with failing hooks | Present the error. Don't retry, don't skip hooks. |
| Running slow checks | Phase 1 lint/test only if <10s. |
| Auto-fixing logic | Triage = trivial only. Logic changes need explicit approval. |
| Creating orphan files | Use existing todo/task files. |
| Massive diff (>500 lines) | Summarize by file. Phase 3: commit by area. Phase 4: audit from summaries. |
| Double-debrief | If last commit starts with `debrief:`, skip Phases 1-3. Audit still runs. |
| Skipping audit on exploration sessions | Discovery sessions with no code changes often produce the richest Context and Intent findings. |
| High context usage | Debrief runs late in sessions. Keep total tool calls under 30. Targeted reads over full file reads. |
```

- [ ] **Step 7: Review full file for word count and completeness**

Read the completed SKILL.md. Verify:
- All 6 phases are covered
- Enhancement lenses are present
- Routing table is present
- Common mistakes section exists
- Word count is under 1500

- [ ] **Step 8: Commit the SKILL.md content**

```bash
git add skills/debrief/SKILL.md
git commit -m "feat: write debrief skill content — 6-phase session-end workflow"
```

---

### Task 3: Create Trigger Evaluation Tests

**Files:**
- Create: `skills/debrief/eval-set.json`

Model after `skills/recon/eval-set.json`. Need both true triggers (session-end scenarios) and false triggers (unrelated tasks).

- [ ] **Step 1: Write eval-set.json**

```json
{
  "tests": [
    { "query": "I'm done for today, let's wrap up this session", "should_trigger": true },
    { "query": "Let's do a debrief before I close out", "should_trigger": true },
    { "query": "Can you clean up any loose ends and update the docs before we stop?", "should_trigger": true },
    { "query": "I'm ending this session, capture what we learned", "should_trigger": true },
    { "query": "Wrap up and commit everything, I'm done", "should_trigger": true },
    { "query": "Let's preserve what we discovered today before closing", "should_trigger": true },
    { "query": "Session's over, update documentation and commit", "should_trigger": true },
    { "query": "Time to close out — any loose ends to tie up?", "should_trigger": true },

    { "query": "Write a python script to parse a CSV file", "should_trigger": false },
    { "query": "Fix the bug in the login component", "should_trigger": false },
    { "query": "What's the current state of this codebase?", "should_trigger": false },
    { "query": "I just started on this project, give me an overview", "should_trigger": false },
    { "query": "Review the pull request", "should_trigger": false },
    { "query": "Help me write unit tests for the payment module", "should_trigger": false },
    { "query": "Create a new README for this project", "should_trigger": false },
    { "query": "Refactor this function to use async/await", "should_trigger": false },
    { "query": "Deploy the app to production", "should_trigger": false },
    { "query": "What does this function do?", "should_trigger": false }
  ]
}
```

Note: "What's the current state of this codebase?" is `false` — that's a `/recon` trigger, not debrief. The boundary between the bookends must be clean.

- [ ] **Step 2: Commit eval-set.json**

```bash
git add skills/debrief/eval-set.json
git commit -m "test: add debrief trigger evaluation test cases"
```

---

### Task 4: Validate, Stats, and Link

**Files:**
- No new files. Uses existing npm scripts.

- [ ] **Step 1: Run validate**

```bash
npm run validate
```

Expected: 0 errors, 0 warnings. If word count warning appears (>1500), go back to Task 2 and compress.

- [ ] **Step 2: Run stats**

```bash
npm run stats
```

Expected: debrief shows up with word count under 1500 and token estimate under ~2500. Compare against recon (1140 words, ~1952 tokens).

- [ ] **Step 3: Link for local testing**

```bash
npm run link
```

Expected: `debrief` symlinked into `~/.claude/skills/`. Verify:

```bash
ls ~/.claude/skills/debrief/
```

Should show `SKILL.md`.

- [ ] **Step 4: Run trigger evaluation**

```bash
npm run eval
```

Expected: All 18 test cases pass. If any fail, adjust the description in SKILL.md frontmatter (trigger conditions) and re-run.

- [ ] **Step 5: Commit any adjustments**

If validation, stats, or eval required changes:

```bash
git add skills/debrief/SKILL.md
git commit -m "fix: adjust debrief skill from validation/eval feedback"
```

If no changes needed, skip this step.

---

### Task 5: Update Project Documentation

**Files:**
- Modify: `CLAUDE.md` (add debrief to Current Skills)
- Modify: `README.md` (add debrief to skill list)

- [ ] **Step 1: Update CLAUDE.md Current Skills section**

Add debrief to the existing list:

```markdown
## Current Skills

- **recon** — Full-spectrum project reconnaissance and doc health audit
- **debrief** — Session-end cleanup, discipline-aware knowledge capture, and documentation routing
```

- [ ] **Step 2: Update README.md skill list**

Read `README.md` first, then add debrief to the skill listing section (match existing format).

- [ ] **Step 3: Commit documentation updates**

```bash
git add CLAUDE.md README.md
git commit -m "docs: add debrief to current skills list"
```

---

### Task 6: Manual Testing Checklist

**No files created.** This is a verification checklist for the implementer.

- [ ] **Step 1: Test invocation in Claude Code**

Open a Claude Code session in any project. Type `/debrief`. Verify:
- The skill loads without errors
- It begins Phase 1 (ASSESS) by checking git status/diff
- It progresses through phases sequentially

- [ ] **Step 2: Test on a session with uncommitted changes**

Make a small change to a file, don't commit. Run `/debrief`. Verify:
- Phase 1 detects the uncommitted change
- Phase 2 categorizes it correctly
- Phase 3 generates a commit with style-matched message
- Phase 6 produces a separate debrief commit

- [ ] **Step 3: Test on a clean session**

Run `/debrief` with no uncommitted changes. Verify:
- Phase 3 skips ("no changes to commit")
- Phase 4 audit still runs
- Report adapts (shorter output)

- [ ] **Step 4: Test double-debrief guard**

Run `/debrief` twice in a row. Verify:
- Second run detects last commit starts with `debrief:`
- Phases 1-3 are skipped
- Phase 4 audit still runs

- [ ] **Step 5: Commit any fixes from testing**

```bash
git add skills/debrief/SKILL.md
git commit -m "fix: debrief skill adjustments from manual testing"
```
